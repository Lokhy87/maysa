{% extends 'base.html.twig' %}

{% block title %}MassageCollection{% endblock %}

{% block body %}
    <h1>MassageCollection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ massage_collection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ massage_collection.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ massage_collection.description }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ massage_collection.icon }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ massage_collection.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_massage_collection_index') }}">back to list</a>

    <a href="{{ path('app_massage_collection_edit', {'id': massage_collection.id}) }}">edit</a>

    {{ include('massage_collection/_delete_form.html.twig') }}
{% endblock %}
