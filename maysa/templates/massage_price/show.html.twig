{% extends 'base.html.twig' %}

{% block title %}MassagePrice{% endblock %}

{% block body %}
    <h1>MassagePrice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ massage_price.id }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ massage_price.duration }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ massage_price.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_massage_price_index') }}">back to list</a>

    <a href="{{ path('app_massage_price_edit', {'id': massage_price.id}) }}">edit</a>

    {{ include('massage_price/_delete_form.html.twig') }}
{% endblock %}
